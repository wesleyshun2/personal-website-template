# 操作手冊 - Markdown 內容系統

我們已成功將 **部落格 (Blog)** 與 **作品集 (Portfolio)** 轉換為動態的 Markdown 系統。現在，您只需上傳 [.md](file:///c:/Users/Arco_asus/.gemini/antigravity/scratch/personal-website/README.md) 檔案即可管理內容。

## 系統重點

### 1. 動態內容管理
您不再需要修改 [.tsx](file:///c:/Users/Arco_asus/.gemini/antigravity/scratch/personal-website/src/app/%5Blocale%5D/page.tsx) 程式碼來新增內容。所有的文章和作品都存放在 `content/` 目錄下。

### 2. Frontmatter 支援
每個 Markdown 檔案頂部都可以包含元數據（標題、日期、圖片等），並使用 `---` 包裹。
範例：
```markdown
---
title: "我的新文章"
date: "2024-03-24"
category: "技術"
excerpt: "這是文章的摘要..."
image: "圖片網址"
---
這裡開始寫文章的正文內容...
```

### 3. 隨選載入 (On-Demand Loading)
只有當訪客點擊特定文章時，系統才會下載並渲染完整的 Markdown 內容，確保首頁與清單頁的載入速度最優化。

## 如何新增內容

### 新增部落格文章
1. 在 `content/[locale]/blog/` 目錄下建立新的 [.md](file:///c:/Users/Arco_asus/.gemini/antigravity/scratch/personal-website/README.md) 檔案（例如：`my-post.md`）。
   - `[locale]` 可以是 [en](file:///c:/Users/Arco_asus/.gemini/antigravity/scratch/personal-website/src/lib/mdx.ts#8-20) 或 `tw`。
2. 在檔案頂部加入必要的 Frontmatter（標題、摘要、日期、分類、圖片）。
3. 在 Frontmatter 下方開始編寫您的 Markdown 內容。

### 新增作品集項目
1. 在 `content/[locale]/portfolio/` 目錄下建立新的 [.md](file:///c:/Users/Arco_asus/.gemini/antigravity/scratch/personal-website/README.md) 檔案（例如：`my-project.md`）。
2. 在檔案頂部加入必要的 Frontmatter（標題、描述、圖片、標籤 tags）。
3. 在下方編寫專案的詳細介紹。

## 目前狀態
- **部落格範例**：位於 `content/[locale]/blog/future-of-minimalist-web-design.md`
- **作品集範例**：位於 `content/[locale]/portfolio/ecommerce-platform.md`

## 實作驗證
- [x] 重構 [src/lib/mdx.ts](file:///c:/Users/Arco_asus/.gemini/antigravity/scratch/personal-website/src/lib/mdx.ts) 以支援元數據提取。
- [x] 更新清單頁面以動態讀取並排序 Markdown 檔案。
- [x] 建立動態路由 (`[slug]`) 以呈現詳細內容。
- [x] 驗證多語言支援（英文與中文）。
